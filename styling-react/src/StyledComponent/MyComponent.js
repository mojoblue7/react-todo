import styled from 'styled-components';

const MyComponent = styled.div`
  font-size: 2rem;
`;

// // 태그의 타입은 styled 함수의 인자로 전달
// export const MyInput = styled('input')`
//   background: gray;
// `;

// // 아예 컴포넌트 형식의 값을 넣어 줌
// export const StyledLink = styled(Link)`
//   color: blue;
// `;

export default MyComponent;
